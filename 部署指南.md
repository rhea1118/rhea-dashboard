# Rhea 工作台 — 5 分钟部署指南

## 先决条件
- 一个 GitHub 账号（免费注册：github.com）
- 把你的 `rhea-dashboard` 文件夹上传到 GitHub 仓库

---

## 方式一：Railway（推荐，最简单，免费额度够用）

### 第 1 步：上传到 GitHub
1. 访问 github.com → 点右上角 + → New repository
2. 仓库名填 `rhea-dashboard`，选 Public
3. 把 `rhea-dashboard` 文件夹里所有文件拖到浏览器上传
4. 点 Commit changes

### 第 2 步：连接 Railway 部署
1. 访问 railway.app → Sign in with GitHub
2. 点 New Project → Deploy from GitHub repo
3. 选择 `rhea-dashboard` 仓库
4. Railway 自动检测 Dockerfile → 自动构建（约 2 分钟）
5. 部署完成后会生成 `https://xxx.up.railway.app` 链接

### 第 3 步：安装到手机
- iPhone：Safari 打开链接 → 底部"分享"→"添加到主屏幕"
- Android：Chrome 打开链接 → 菜单 →"添加到主屏幕"

---

## 方式二：Render（备选）

1. 访问 render.com → Sign in with GitHub
2. New Web Service → 连接 rhea-dashboard 仓库
3. Render 自动检测 render.yaml 配置
4. 等待部署 → 获得 `https://xxx.onrender.com` 链接

---

## 方式三：自己电脑运行（开发调试）

```bash
# 安装依赖
cd rhea-dashboard
python -m venv .venv
.venv\Scripts\pip install requests feedparser beautifulsoup4

# 启动服务器
node server.js

# 浏览器打开：http://localhost:8888
```

---

## 验证部署成功

部署后访问你获得的 URL，应该看到：
- 🏠 首页概览正常显示
- 📰 切换"外贸运营"→"热点速报"→点"🔍 实时搜索生成简报"
- ⏳ 等待 10-30 秒 → AI 生成的热点速报显示出来

---

## 项目包含的能力

| 模块 | 说明 |
|---|---|
| 每日待办 | 工作/生活分类管理 |
| 外贸运营 | **支持实时 AI 生成**：汇率/非洲新闻/南美新闻/竞品动态/社媒热点 + 自动选题策划 |
| 学习阅读 | 英语/阅读/理财追踪 |
| 收支记账 | 自动分类统计 |
| 减脂记录 | 体重/体脂 + SVG 趋势图 |
| 日程安排 | 今日 + 未来日程 |
| 纪念日 | 公历/农历倒计时 |
| 经期记录 | 周期预测 + 月历 |
